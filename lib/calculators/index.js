import calculateBirthingDate from './birthing-date';
import calculateBreedingDate from './breeding-date';
import calculateDaysOnFeed from './days-on-feed';
import calculateWeightGain from './weight-gain';
import calculateAverageDailyGain from './average-daily-gain';
import calculateAdjustedBirthWeight from './adj-birth-weight';
import calculateAdjustedWeaningWeight from './adj-weaning-weight';
import calculateAdjustedYearlingWeight from './adj-yearling-weight';
import calculateDeathLoss from './death-loss';
import calculateBirthingSeason from './birthing-season';
import calculateWeightWeaned from './weight-weaned';
import calculateBirthingPercentage from './birthing-percentage';
import calculateWeaningPercentage from './weaning-percentage';
import calculateCattleReplacementsNeeded from './cattle-replacements-needed';
import calculateFeedCost from './feed-cost';

export default {
    calculateBirthingDate,
    calculateBreedingDate,
    calculateDaysOnFeed,
    calculateWeightGain,
    calculateAverageDailyGain,
    calculateAdjustedBirthWeight,
    calculateAdjustedWeaningWeight,
    calculateAdjustedYearlingWeight,
    calculateDeathLoss,
    calculateBirthingSeason,
    calculateWeightWeaned,
    calculateBirthingPercentage,
    calculateWeaningPercentage,
    calculateCattleReplacementsNeeded,
    calculateFeedCost
}